# /etc/fail2ban/filter.d/sip-auth-ip.conf
#
# Filtro vazio — bans são injetados diretamente pelo event_guard_daemon
# via: fail2ban-client set sip-auth-ip banip <ip>
# Nenhum regex necessário.

[Definition]
failregex =
ignoreregex =
