# /etc/fail2ban/filter.d/sip-auth-fail.conf
#
# Filtro vazio — bans são injetados diretamente pelo event_guard_daemon
# via: fail2ban-client set sip-auth-fail banip <ip>
# Nenhum regex necessário.

[Definition]
failregex =
ignoreregex =
